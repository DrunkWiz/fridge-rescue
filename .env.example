# Copy to .env and fill in. .env is gitignored — never commit real keys.
# Recipe generation (Day 2)
EXPO_PUBLIC_RECIPE_API_KEY=
# RevenueCat Test Store API key (Day 4)
EXPO_PUBLIC_REVENUECAT_API_KEY=
